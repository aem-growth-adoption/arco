{%- if accessories | length == 0 -%}
(none)
{%- else -%}
{%- for a in accessories %}
- **{{ a.name }}** (ID: {{ a.id }}) | ${{ a.price or '?' }} | {{ a._description }}{% if not loop.last %}
{% endif %}{%- endfor -%}
{%- endif -%}